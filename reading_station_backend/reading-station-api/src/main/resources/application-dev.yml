spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

  devtools:
    restart:
      enabled: true

logging:
  level:
    com.tramdoc: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG

server:
  port: 8080
